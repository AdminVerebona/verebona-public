# PREPROD (utiliser `npm run build:preprod`, soit `vite build --mode preprod`)
# FULL par defaut ; ?mode=prelaunch affiche la version publiee en production.
VITE_APP_URL=https://app.preprod.verebona.fr
VITE_ENVIRONMENT=preprod
VITE_DEFAULT_SITE_MODE=full
